//! Shared bench helpers for `paksmith-bench`.
//!
//! Currently a placeholder — bench-helper code (cached-fixture
//! generators, throughput-size helpers) lands here as benches grow.
//! Kept as a `[lib]` so multiple bench binaries can share setup
//! without duplicating it.
//!
//! **Stability:** internal to `paksmith-bench`. Not published; not
//! intended for downstream consumers.

#![allow(missing_docs)]
