//! `required-headings` linter: stub. Real implementation lands in
//! the next commit (Task 2).

use anyhow::Result;
use std::path::Path;

pub fn check_dir(_dir: &Path) -> Result<()> {
    anyhow::bail!("not yet implemented")
}
