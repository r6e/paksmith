[package]
name = "paksmith-gui"
version = "0.1.0"
edition.workspace = true
license.workspace = true
rust-version.workspace = true
description = "GUI for exploring and extracting Unreal Engine game assets"

[dependencies]
# Phase 6 (per ROADMAP) will add `iced` and `paksmith-core` here
# when the first iced screen lands. Until then the GUI is a 5-line
# placeholder and dragging in iced (very heavy) just to print one
# string adds tens of seconds per `cargo build --workspace`. See
# issue #139.

[lints]
workspace = true
